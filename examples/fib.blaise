program Fibonacci;

var n : Integer;

function fib(n : Integer) : Integer
begin
	if n = 0 then
		result := 0
	else if n = 1 then
		result := 1
	else
		result := fib(n-1) + fib(n-2)
end;

begin
	write("N? ");
	readln(n);
	writeln("Resultado = ", fib(n))
end.
